# Phase Plan: Shadow Mapping — Phase 1: Hard Shadows

**Gate**: Feature Brief approved (Phase 1 gate)
**Roadmap alignment**: Phase 1 — Hard Shadows (see brief.md)
**Build target**: `cmake --build build --target renderer_app`
**Groomer Verdict**: PASS
**Revision**: r4 — resolved all three third-groomer defects via phase split: T-7 and T-8 (demo scene + debug overlay) removed from this plan; Phase 1 is now T1–T6 (core hard-shadow pipeline only). This also resolves defects 1 (T-7 weak acceptance) and 2 (T-8 ‖ overlap with T-7) since those tasks no longer exist in this phase. NOTE: brief.md still lists demo scene and debug overlay under Phase 1 scope — human must update brief.md (move those items to Phase 2) before plan-p2.md is written.

## Expected Outcome

Running `renderer_app` builds and starts without errors. The shadow pipeline infrastructure is in place: `BlinnPhongShadowed` is a valid enum value in `renderer.h`, the shadow depth pipeline and shadow map GPU resources initialize cleanly (no sokol validation errors), the light-space matrix function computes correctly, and the shadow pass executes in `renderer_end_frame()` before the main pass. The existing renderer demo renders identically to before. Visual shadow effects and the demo scene are delivered in Phase 2.

---

## Tasks

| ID | Status | Description | Files | Schedule | Acceptance |
|----|--------|-------------|-------|----------|------------|
| 1 | [x] | **Interface bump (approved 2026-05-16)**: Add `BlinnPhongShadowed = 3` to `BuiltinShader` enum in `renderer.h`. Add `renderer_make_blinnphong_shadowed_material(const float rgb[3], float shininess, RendererTextureHandle texture = {})` declaration to `renderer.h`. Add `BlinnPhongShadowedFSParams` note in comments pointing to `BlinnPhongFSParams` reuse (no new struct needed — FS material params are identical; shadow data is renderer-internal at binding=2). Bump `renderer-interface-spec.md` to v1.3, add changelog entry. | `src/renderer/renderer.h`, `docs/interfaces/renderer-interface-spec.md` | GATE | `renderer.h` compiles; `BuiltinShader::BlinnPhongShadowed` is accessible; spec shows v1.3 |
| 2 | [x] | **Shadow depth shader + pipeline**: Write `shaders/renderer/shadow_depth.glsl`. VS: `@glsl_options fixup_clipspace` (maps GL clip-z from [-1,1] to [0,1] for correct depth storage), `layout(binding=0) uniform ShadowVSParams { mat4 light_mvp; };`, input `in vec4 pos;` only. FS: empty (`void main() {}`). Add `@program shadow_depth vs_shadow fs_shadow`. In `pipeline_shadow.cpp`, create pipeline: vertex layout stride = `sizeof(Vertex)` (44 bytes = 11 floats: position[3] + normal[3] + uv[2] + tangent[3]), only position attr at offset 0 (`FLOAT3`); `cull_mode = SG_CULLMODE_FRONT` (back-face-lit shadow pass — eliminates acne without static bias, at cost of Peter Panning on very thin geometry, acceptable for MVP geometry); `depth.pixel_format = SG_PIXELFORMAT_DEPTH`; `depth.compare = SG_COMPAREFUNC_LESS_EQUAL`; `depth.write_enabled = true`; `colors[0].pixel_format = SG_PIXELFORMAT_NONE`. Add sokol-shdc CMake custom command for `shadow_depth.glsl`. | `shaders/renderer/shadow_depth.glsl`, `src/renderer/pipeline_shadow.cpp`, `src/renderer/pipeline_shadow.h`, `CMakeLists.txt` (renderer lib) | ‖ | `cmake --build build --target renderer_app` exits 0; no sokol validation error for shadow pipeline creation; pipeline logged as created — `create_pipeline_shadow()` called from `renderer_internal_init()`, result stored in `state.pipeline_shadow`, success/warning logged |
| 3 | [x] | **Shadow map GPU resources**: In `shadow_pass.cpp`, implement `shadow_pass_init()`. Verify `sg_make_view` with `depth_stencil_attachment` exists in pinned sokol headers before writing (grep `sg_view_desc` in vendor); adapt API call if needed (escalate if not present). Create: `shadow_map_img` — `sg_make_image` with `usage.depth_stencil_attachment = true`, `width/height = 2048`, `pixel_format = SG_PIXELFORMAT_DEPTH`, `sample_count = 1`. Create two views: `shadow_ds_view = sg_make_view({.depth_stencil_attachment = {.image = shadow_map_img}})` (write target) and `shadow_tex_view = sg_make_view({.texture = {.image = shadow_map_img}})` (read target for display pass). Create `shadow_sampler` with `compare = SG_COMPAREFUNC_LESS`, `min/mag_filter = SG_FILTER_LINEAR`, `wrap = SG_WRAP_CLAMP_TO_EDGE`. Also create `shadow_attachments = sg_make_attachments({.depth_stencil = {.view = shadow_ds_view}})` and store the resulting `sg_attachments` handle in `ShadowPassState` — T-6 calls `sg_begin_pass` with this handle, so the struct must own it. Build `ShadowPassState` holding: image, both views, sampler, and attachments handle. Expose `shadow_pass_init()`, `shadow_pass_shutdown()`, and `shadow_pass_state()` from the header. Call `shadow_pass_init()` from `renderer_init()` after `sg_setup()`. | `src/renderer/shadow_pass.cpp`, `src/renderer/shadow_pass.h`, `src/renderer/renderer.cpp` | ‖ with 2 | Init completes without sokol validation layer errors; shadow map image handle is non-zero |
| 4 | [x] | **BlinnPhongShadowed shader + pipeline + factory**: Write `shaders/renderer/blinnphong_shadowed.glsl`. VS block: same `layout(binding=0)` 192-byte block as blinnphong (`mat4 mvp; mat4 model; mat4 normal_mat;`) — outputs `world_pos` and `world_norm` to FS. FS: `layout(binding=1) uniform BlinnPhongFSParams { ... }` (identical to blinnphong.glsl binding 1 block); `layout(binding=2) uniform ShadowFrameParams { mat4 light_view_proj; };`; `layout(binding=0) uniform texture2D shadow_map; layout(binding=0) uniform sampler shadow_sampler`. Albedo tex/sampler at bindings 4/5 (shifted from blinnphong's 2/3 since binding 2 holds shadow_frame_params). Shadow sampling: in FS, compute `vec4 lp = light_view_proj * vec4(world_pos, 1.0); vec3 shadow_uv = vec3((lp.xy / lp.w) * 0.5 + 0.5, lp.z / lp.w);` then `float shadow_factor = texture(sampler2DShadow(shadow_map, shadow_sampler), shadow_uv);`. Apply as: `diffuse *= shadow_factor; specular *= shadow_factor;` (ambient is NOT modulated — objects in shadow still have ambient). Add `#if !SOKOL_GLSL` Y-flip guard for non-GL backends (mirrors sokol sample pattern). In `pipeline_blinnphong_shadowed.cpp`, create pipeline matching blinnphong pipeline config. Implement `renderer_make_blinnphong_shadowed_material()` in `renderer.cpp` (same as blinnphong variant, sets `m.shader = renderer_builtin_shader(BuiltinShader::BlinnPhongShadowed)`). CMake auto-scans via `vibeaton_attach_shader_headers` (GLOB on `*.glsl`). Gotcha: `light_view_proj` and shadow bindings are set per-frame by the renderer at binding=0/view=0/sampler=0 for all `BlinnPhongShadowed` draws — the game never touches these. | `shaders/renderer/blinnphong_shadowed.glsl`, `src/renderer/pipeline_blinnphong_shadowed.cpp`, `src/renderer/pipeline_blinnphong_shadowed.h`, `src/renderer/renderer.cpp` (factory impl) | → | sokol-shdc compiles `blinnphong_shadowed.glsl` cleanly; pipeline created; `renderer_make_blinnphong_shadowed_material()` callable |
| 5 | [x] | **Light-space matrix computation**: In `shadow_pass.cpp`, add `glm::mat4 shadow_compute_light_view_proj(const DirectionalLight& light, float ortho_half_size, float ortho_near, float ortho_far)`. Light "eye" position: `scene_center + dir * eye_dist` where `dir = normalize(light.direction)` (surface→light convention matching blinnphong.glsl's `light_dir_ws`) and `eye_dist = ortho_far * 0.5f`, `scene_center = {0,0,0}`. Light view: `glm::lookAt(eye_pos, scene_center, up_vector)` — up vector selection: if `abs(dot(normalize(dir), {0,1,0})) > 0.99`, use `{1,0,0}` as up, else `{0,1,0}`. Light proj: `glm::ortho(-half, half, -half, half, near, far)` (symmetric). Phase 1 hardcoded constants in `shadow_pass.h`: `k_shadow_map_size = 2048`, `k_shadow_ortho_half_size = 600.0f` (covers ~1.2 km diameter for game arena; demo scene uses a tighter override), `k_shadow_near = -1200.0f`, `k_shadow_far = 1200.0f`. Expose `shadow_compute_light_view_proj()` for use in renderer's end_frame. Note: future Phase 3 adds `renderer_set_shadow_config()` replacing these constants; architecture is ready (single function call site). | `src/renderer/shadow_pass.cpp`, `src/renderer/shadow_pass.h` | → | For demo-scene light `direction = normalize({0.3, -1.0, 0.2})`: all 16 matrix elements are finite (no NaN, no Inf); scene origin `{0,0,0}` maps to clip-space XY in `[-1,1]` and clip-space Z in `[0,1]` after perspective division (i.e., origin is inside the ortho frustum). For near-vertical `direction = {0,-1,0}`: function produces a finite, non-degenerate matrix (up-vector fallback `{1,0,0}` fires without NaN). Verified via unit tests in `src/renderer/tests/test_shadow_pass.cpp` (9 tests, 13 assertions, all pass). |
| 6 | [x] | **Shadow pass execution + per-draw binding**: In `renderer.cpp`: (a) At the start of `renderer_end_frame()`, BEFORE `sg_begin_pass()` for the swapchain, compute `light_view_proj = shadow_compute_light_view_proj(current_light, ...)` once per frame. If light is set and shadow pipeline is valid, begin shadow pass via `sg_pass { .action = ..., .attachments = sps->shadow_attachments }` then `sg_begin_pass()`, apply shadow pipeline, iterate all `render_queue=0` draws: `sg_apply_bindings({.vertex_buffers[0]=draw.vbuf, .index_buffer=draw.ibuf})`, `sg_apply_uniforms(UB_shadow_depth_vs_params, {.light_mvp = light_view_proj * glm::make_mat4(draw.world_transform)})`, `sg_draw(0, draw.index_count, 1)`, `sg_end_pass()`. (b) After shadow pass ends, begin swapchain pass: `sg_pass { .action = ..., .swapchain = sglue_swapchain() }` then `sg_begin_pass()` for main rendering. (c) During main pass dispatch, when `draw.material.shader == BlinnPhongShadowed` handle: add `sg_bindings.views[VIEW_shadow_map] = shadow_state.tex_view` and `sg_bindings.samplers[SMP_shadow_sampler] = shadow_state.sampler`; reuse the precomputed `light_view_proj` (computed once at top of end_frame) to apply `ShadowFrameParams {light_view_proj}` as uniform binding 2 for EVERY `BlinnPhongShadowed` draw. Gotcha: use sokol-shdc generated constants (`UB_blinnphong_shadowed_shadow_frame_params`, `UB_shadow_depth_vs_params`) instead of hardcoded indices; the pinned sokol version uses `sg_pass.attachments` API, not a two-arg `sg_begin_pass(attachments, action)`. | `src/renderer/renderer.cpp` | → | Shadow pass executes before main pass; no sokol validation errors; draw ordering preserved; no per-frame heap allocations (shadow pass loop only accesses the pre-built static draw queue — no `new`, `malloc`, or growing containers in the hot path); `light_view_proj` computed once per frame, not per-draw |
| 7 | — | **DEFERRED to Phase 2**: Shadow demo scene (ShadowDemo class, ImGui tab, orbiting camera, BlinnPhongShadowed objects, comparison sphere, transparent sphere). Spec retained in Phase 2 plan. | — | — | — |
| 8 | — | **DEFERRED to Phase 2**: Shadow map debug overlay (shadow_debug.glsl, shadow_debug_init/draw in shadow_pass.cpp, "Show shadow map" ImGui checkbox in ShadowDemo). Spec retained in Phase 2 plan. | — | — | — |

**Schedule codes**:
- `GATE` — runs alone first; all subsequent tasks depend on it
- `‖` — parallel with adjacent `‖` tasks; listed Files are disjoint
- `→` — sequential after all preceding tasks complete

---

## Fallbacks

- **T-3 (sg_make_view API mismatch)**: If `sg_view_desc.depth_stencil_attachment` does not exist in the pinned sokol version, check for `sg_attachments` / `sg_make_attachments` API (older sokol pattern) and adapt accordingly. Use `sg_begin_pass` with the attachment-based variant. Escalate to human if neither API is available.
- **T-4 (sampler2DShadow binding)**: If sokol-shdc rejects combined `texture2D + sampler` with `compare = SG_COMPAREFUNC_LESS` on the GL backend, fall back to manual PCF (4-sample manual depth comparison in FS) using a regular `sampler2D`. This is a softer shadow edge but functionally correct for Phase 1.
- **T-6 (ShadowFrameParams binding index)**: If the generated uniform binding index for `ShadowFrameParams` does not match `UB_shadow_depth_vs_params` (naming collision with shadow VS uniforms), use the sokol-shdc generated constants (`UB_blinnphong_shadowed_shadow_frame_params` or equivalent) rather than a hardcoded `2`.

---

## Human Checkpoint

**Run**: `cmake --build build --target renderer_app && ./build/renderer_app`

**Look for**:
1. Build exits 0 with no new warnings.
2. Application starts; console shows shadow pass init completed — shadow map image handle non-zero; shadow depth pipeline created without sokol validation error.
3. No sokol validation errors during frame execution (shadow pass running before main pass each frame).
4. Existing renderer demo renders identically to before — no visual regression, no new GL errors.

**Pass**: All 4 criteria met. The core shadow pipeline is wired and running; visual shadows and the demo scene are validated in Phase 2.

**Stop**: Any of the following — build exits non-zero, sokol validation error on shadow pipeline or shadow map resource creation, existing demo visually broken or crashing, GL errors reported.
