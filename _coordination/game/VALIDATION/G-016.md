# VALIDATION — G-016: Projectile Lifetime + Collision Despawn

**Task**: Implement projectile lifetime + collision despawn in `src/game/projectile.h` / `src/game/projectile.cpp`
**Validation level**: SELF-CHECK
**Validated by**: Spec Validator (automated review)
**Date**: 2026-04-26

---

## Acceptance Criteria

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | `projectile_update(dt)` iterates `view<ProjectileTag, ProjectileData>()` | PASS | Lines 7–8: correct view type |
| 2 | Skips entities already marked DestroyPending | PASS | Lines 11–12: `reg.all_of<DestroyPending>(e)` continue |
| 3 | Checks lifetime: `engine_now() - spawn_time > lifetime` | PASS | Lines 15–17: computes age as double, compares against float lifetime |
| 4 | Marks expired projectiles with DestroyPending | PASS | Line 18: `reg.emplace<DestroyPending>(e)` |
| 5 | Does not handle collision despawn (deferred to G-020) | PASS | Only lifetime-driven cleanup; collision despawn is in damage.cpp |
| 6 | Files match declared set: `projectile.h`, `projectile.cpp` | PASS | Disjoint from G-015 and G-017 file sets |

## Consistency with Specs

- **game-interface-spec.md** §System Execution Order: step 6 (`projectile_update`) — correctly positioned in game.cpp tick
- **GAME_DESIGN.md**: Plasma projectile lifetime 3s, despawn on collision or expiry — matches
- **game-architecture.md** §Data Model: `ProjectileData { owner, damage, spawn_time, lifetime }` — all fields present and used correctly

## Build Verification

```
cmake --build build --target game → SUCCESS (no warnings)
```

---

**Verdict**: PASS — all acceptance criteria met. Clean 20-line implementation that handles lifetime-driven cleanup correctly and defers collision despawn to G-020's `damage_resolve()`.
