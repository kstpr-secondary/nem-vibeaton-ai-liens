# VALIDATION — G-015: Weapon Definitions and Switching

**Task**: Implement weapon definitions and switching in `src/game/weapons.h` / `src/game/weapons.cpp`
**Validation level**: SELF-CHECK
**Validated by**: Spec Validator (automated review)
**Date**: 2026-04-26

---

## Acceptance Criteria

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | `weapon_ready()` checks active weapon cooldown against `engine_now()` | PASS | Lines 21–26: compares `now - ws.laser_last_fire` or `now - ws.plasma_last_fire` against `ws.laser_cooldown` / `ws.plasma_cooldown` |
| 2 | Q/E edge-triggered weapon switch via `prev_q`/`prev_e` statics | PASS | Lines 87–97: static `s_prev_q`/`s_prev_e`, detects rising edge `cur_q && !s_prev_q` |
| 3 | `weapon_update(dt)` iterates `PlayerTag` + `Transform` + `WeaponState` view | PASS | Lines 105–124: correct view type, single-player iteration |
| 4 | Weapon switch writes `WeaponState.active_weapon` (Laser/Plasma) | PASS | Lines 112–115: sets `active_weapon = WeaponType::Laser` or `Plasma` |
| 5 | Fire input gated by right mouse button + cooldown | PASS | Line 99: `fire = engine_mouse_button(1)`; line 118: `fire && weapon_ready(ws)` |
| 6 | Laser fire path implemented (raycast + line visual + damage) | PASS | `laser_fire()` at lines 32–58: raycast, apply_damage, impulse on asteroids, line quad enqueue |
| 7 | Plasma fire path implemented (projectile spawn) | PASS | `plasma_fire()` at lines 67–77: spawn position offset, velocity, calls `spawn_projectile` |
| 8 | Cooldown timestamps updated (`laser_last_fire`, `plasma_last_fire`) | PASS | Lines 57 + 76 |
| 9 | Files match declared set: `weapons.h`, `weapons.cpp` | PASS | Disjoint from G-016 and G-017 file sets |

## Consistency with Specs

- **game-interface-spec.md** §Input Mapping: Q → Plasma, E → Laser (edge-triggered) — matches
- **game-interface-spec.md** §System Execution Order: step 5 (`weapon_update`) — correctly positioned in game.cpp tick
- **GAME_DESIGN.md** Weapons section: Laser 5s cooldown, 10 dmg, instant hit; Plasma 0.1s cooldown, 0.5 dmg, projectile — matches constants.h and WeaponState defaults
- **game-architecture.md** Module Map: `weapons.h`/`weapons.cpp` responsibility matches spec

## Build Verification

```
cmake --build build --target game → SUCCESS (no warnings)
```

---

**Verdict**: PASS — all acceptance criteria met. Code compiles without warnings, implements the full weapon system including laser raycast and plasma projectile paths in a single file per sequential constraint with G-018/G-019.
