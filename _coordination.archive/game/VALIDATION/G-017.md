# VALIDATION — G-017: Game-Local Enemy AI

**Task**: Implement game-local enemy AI in `src/game/enemy_ai.h` / `src/game/enemy_ai.cpp`
**Validation level**: SPEC-VALIDATE
**Validated by**: Spec Validator (automated review)
**Date**: 2026-04-26

---

## Acceptance Criteria

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | `enemy_ai_update(dt)` iterates `view<EnemyTag, Transform, EnemyAI, WeaponState>()` | PASS | Line 28: view includes RigidBody (needed for velocity write) — correct superset |
| 2 | Computes seek vector: `normalize(player_pos - enemy_pos) * pursuit_speed` | PASS | Lines 35–41: `to_player / dist * ai.pursuit_speed` written to `rb.linear_velocity` |
| 3 | Stops near player (avoids physics twitching) | PASS | Lines 47–51: if `dist > 5.0` sets velocity, else dampens by 0.9× |
| 4 | Faces player via rotation | PASS | Line 54: `quatLookAtRH(dir, vec3(0,1,0))` — up is world Y |
| 5 | Kills angular velocity (AI-controlled rotation only) | PASS | Line 57: `rb.angular_velocity = vec3(0)` |
| 6 | LOS check via `engine_raycast(enemy_pos, dir, dist)` | PASS | Line 72: raycast from enemy position along direction to player |
| 7 | Only fires if hit entity is the player | PASS | Lines 73–74: `hit->entity != player_e` → continue (blocked shot) |
| 8 | Respects fire range (`dist > ai.fire_range`) | PASS | Line 62: early continue if too far |
| 9 | Respects fire cooldown | PASS | Lines 65–67: `(now - ai.last_fire_time) < ai.fire_cooldown` → continue |
| 10 | Spawns plasma projectile with correct spawn position + velocity | PASS | Lines 77–83: offset by ship half + radius, inherits enemy linear_velocity + forward component |
| 11 | Updates `last_fire_time` after firing | PASS | Line 84: `ai.last_fire_time = now` |
| 12 | Handles missing player gracefully (early return) | PASS | Lines 17–19: empty player view → return |
| 13 | Does not depend on engine E-M5 (stays game-local) | PASS | Pure game-local code, only uses `engine_raycast` (E-M3) and `spawn_projectile` (game) |
| 14 | Files match declared set: `enemy_ai.h`, `enemy_ai.cpp` | PASS | Disjoint from G-015 and G-016 file sets |

## Consistency with Specs

- **game-interface-spec.md** §System Execution Order: step 4 (`enemy_ai_update`) — correctly positioned in game.cpp tick between player_update (3) and weapon_update (5)
- **game-interface-spec.md** §Input Mapping: enemy AI does not poll input — correct, it's autonomous
- **GAME_DESIGN.md** Enemies section: "steer toward player at fixed speed, fire plasma when within range + line-of-sight" — matches exactly
- **game-architecture.md** R5: "Game-local enemy AI for MVP — Seek + LOS + cooldown is ~20 lines" — implementation is 86 lines (includes velocity inheritance and rotation), reasonable deviation
- **game-architecture.md** §Data Model: `EnemyAI { pursuit_speed, fire_range, fire_cooldown, last_fire_time }` — all fields used correctly per constants.h defaults

## Spec Deviations

| # | Deviation | Rationale |
|---|-----------|-----------|
| 1 | View includes `RigidBody` in addition to declared `EnemyTag, Transform, EnemyAI, WeaponState` | Required to write `linear_velocity`; the spec says "pick one and document" — chose direct velocity assignment over `engine_apply_force` for deterministic AI control |
| 2 | Enemy faces player via `quatLookAtRH` (visual alignment) | Not in spec but good practice; ensures enemy visual cube points toward player for consistent firing |
| 3 | Inherit enemy velocity in projectile: `rb.linear_velocity + dir * ws.plasma_speed` | Enhancement over spec minimal "spawn aimed at player"; makes enemy movement affect shot trajectory, improving gameplay feel |
| 4 | Dampens velocity when close (`*= 0.9`) instead of zeroing | Prevents jitter from physics collision response fighting AI velocity assignment |

## Build Verification

```
cmake --build build --target game → SUCCESS (no warnings)
```

---

**Verdict**: PASS — all acceptance criteria met. Implementation correctly follows the spec's core requirements (seek + LOS + cooldown-gated plasma fire) with reasonable enhancements for gameplay quality. No dependency on engine E-M5. Code is clean, well-structured, and fits within expected scope.
